# 任务

1. 清理 tsconfig.json 和 components.json 中无用或冲突的路径配置，确保 `~/` 指向 `src` 文件夹。
2. 统一代码中所有 import 语句，尽量使用 `~/` 作为根路径前缀。对于引用偏向全局可用的模块时，禁止使用相对路径如 `./` 或 `../`。如果某个文件要引用同一模块、同一文件夹/子文件夹、或者附近文件夹的另一个文件，也是可以使用相对路径的。
3. 在项目文档中新增一节，详细说明 import 路径的配置和使用规范，示例如何正确导入模块。
4. 确保所有代码符合此规范，避免路径混乱导致的导入错误或维护困难。
