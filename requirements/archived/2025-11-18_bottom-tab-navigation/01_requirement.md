我现在想修改这个应用的导航，把这个应用做成一个通过底部tabs来切换页面的样子。

不管哪个页面，header区域关于钱包链接这部分保持不变。

底部tab栏有三个：
- 左边 list：点击打开现在的 X402 List 页（应用打开的时候展示这页）
- 中间一个加号按钮，点击后相当于点击现在 enter x402 url 按钮的效果，不做tab页切换，还是和现在一样弹出一个底部弹窗
- 右边 history：点击打开一个 history payments 页。这个页之后会记录已经完成的 payments，不过现在先用一个空页面代表即可。