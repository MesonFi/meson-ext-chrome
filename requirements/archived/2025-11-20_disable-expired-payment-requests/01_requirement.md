# 需求

1. 在“历史”标签页中，如果打开的记录已过期，则“Request with Payment Header”按钮应禁用，防止重复请求。  
2. 如果打开记录时按钮尚未禁用，但在发送请求时检测到支付头已过期，应：
   - 更新按钮状态为禁用。  
   - 通过可见提示（例如 Sonner 的 `toast.error("支付请求已过期")` 或在表单上方渲染错误信息）告知用户“支付请求已过期”。
